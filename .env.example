# InfluxDB connection
# Use https:// if InfluxDB is on a remote host to protect the API token in transit
INFLUXDB_URL=http://localhost:8086
INFLUXDB_TOKEN=your-read-only-token-here
INFLUXDB_ORG=your-org
INFLUXDB_BUCKET=home_assistant

# Flux query filters
MEASUREMENT=http_listener_v2
FIELD=temperature
DEVICE_ID=gisebo-01
HOST_FILTER=61781446e5e9

# Remote host (SSH/SCP)
REMOTE_USER=youruser
REMOTE_HOST=yourhost.example.com
REMOTE_PATH=/var/www/html/data/temperature.json

# Timeout (optional)
# TIMEOUT_SECONDS=30
